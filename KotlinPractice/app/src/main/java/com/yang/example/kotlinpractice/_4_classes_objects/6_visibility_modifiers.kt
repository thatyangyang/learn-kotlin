package com.yang.example.kotlinpractice._4_classes_objects

/**
 * 包内
 *  默认 public
 *
 * 类成员
 *  在 Kotlin 中，外部类不能访问内部类的 private 成员。
 *  默认是 public
 *
 * 构造函数
 *  默认情况下，所有构造函数都是公开（public）的
 *
 * 局部声明
 *  局部变量、函数和类不能有可见性修饰符。
 *
 * 模块
 * 可见性修饰符 internal 意味着该成员只在相同模块内可见。更具体地说， 一个模块是编译在一起的一套 Kotlin 文件
 */
fun main() {
    
}